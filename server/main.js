import { Meteor } from 'meteor/meteor';
//import { WebApp } from 'meteor/webapp'; // allow attach middleware with method use
import './../imports/api/users';
import './../imports/startup/simple-schema-configuration';

Meteor.startup(() => {

});
